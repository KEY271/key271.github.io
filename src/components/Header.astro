---
import { getRelativeLocaleUrl } from "astro:i18n";
import { ui, type Locale } from "../i18n/ui";
import LanguageSwitcher from "./LanguageSwitcher.astro";

type Props = {
    locale: Locale;
};

const { locale } = Astro.props;
const t = ui[locale];
---

<header>
    <div class="site-title">
        <a href={getRelativeLocaleUrl(locale, "")}>Keita Tsuji</a>
    </div>
    <nav>
        <a href={getRelativeLocaleUrl(locale, "cv")}>{t.nav_cv}</a>
        <LanguageSwitcher locale={locale} />
    </nav>
</header>

<style>
    header {
        display: flex;
        background-color: #3d5a80;
        height: 4em;
        align-items: center;
        justify-content: space-between;
        padding: 0 1em;
    }
    .site-title {
        font-weight: bold;
        font-size: 20px;
    }
    nav {
        display: flex;
        gap: 1em;
    }
    a {
        text-decoration: none;
        color: white;
    }
</style>
